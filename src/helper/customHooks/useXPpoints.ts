function useXPpoints() {
  // when ever the user needs to add points to the user.
  // Find out about the user's XP points from the database.
  // This should be for users who are registered.
  return {
    addPoints(points: number) {},
  }
}